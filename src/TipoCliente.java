// Lista con todos los tipos de clientes que tiene el restaurante
// Si se quieren añadir más tipos de clientes, solo hay que modificar esta clase

public enum TipoCliente {
    REGULAR, VIP, EMPLEADO
}
